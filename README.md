Program 1

Design a web page for the department timetable containing a suitable header, footer, description of the course codes and titles, faculty name and initials, etc. Use href, list tags and add University logo as a background. Using table tag and additional features like spanning rows, columns and table borders. [Refer your class timetable]

Program 2(a)

Create a form to collect Student Feedback. (Use textbox, textarea, checkbox, radio button, select box etc.)

Program 2(b)

Create a web page using frame. Divide the page into two parts with Navigation links on left hand side of page (width = 20%) and content page on right hand side of page (width = 80%). On clicking the navigation links corresponding content must be shown on the right-hand side.

Program 3(a)

Create a web page using frame. Divide the page into two parts with Navigation links on left hand side of page (width = 20%) and content page on right hand side of page (width = 80%). On clicking the navigation links corresponding content must be shown on the right-hand side.

Program 3(b)

Write HTML code to develop a webpage having two frames that divide the webpage into two equal rows and then divide the row into equal columns. Fill each frame with a different background colour.

Program 4

Create your resume using HTML tags. Also experiment with colors, text (bold, italic and different headings), image, link, size and other related tags.

Program 5

Design a web page using CSS with suitable design for the following:

Demonstrate different font styles.
Control the repetition of image with background-repeat property.
Define style for links as a:link, a:active, a:hover, a:visited.
Demonstrate element visibility property.
Program 6(a)

Design a web page of your institute (Week-1) with an attractive background color, text color, image, font etc. (Use Internal CSS). Use External CSS to format the class timetable.

Program 6(b)

Use External, Internal and Inline CSS to format the resume that you created in Week-4.

Program 7(a)

Develop a simple calculator for Addition, Subtraction, Multiplication and Division operations using JavaScript.

Program 7(b)

Create an HTML page that contains a form with fields Name, Email, Mobile Number, Gender, Favorite Color and a button. Write JavaScript code to validate all the fields when the button is clicked, then combine and display the information in a textbox.

Program 8

Write an XHTML document which displays a form containing text elements to input Register Number, Subject Code, Marks in three tests and a button element. Also write JavaScript to compute the average of the best two tests on click of button and print average marks using alert. Validate all the fields using JavaScript.

Program 9

Develop an XHTML document and JavaScript to validate the following fields in a registration page:

User ID (must be of length 5 to 12).
Name (only alphabets and length should not be less than 15 characters).
Password (must be eight characters including one uppercase letter, one special character and alphanumeric characters).
E-mail (should not contain invalid addresses).
Program 10(a)

Write a PHP script to display today's date in dd-mm-yyyy format.

Program 10(b)

Write a PHP script to check whether the number is prime or not when user inputs a valid number from client side.

Program 11

Create an HTML page that contains textbox, submit/reset button. Write a PHP script to display this information and also store it into a text file.

Program 12

Write a PHP script for login authentication. Design an HTML form which takes Username and Password from user and validate against stored Username and Password in a file.